@using MudBlazor
@inherits RubricNodeBodyComponent

<span class="font-monospace" style="justify-self: start;" @onclick:stopPropagation="true">
    @Node.Text
    @if (IsBusyProgress)
    {
        <span class="loader ms-2"></span>
    }
</span>
<MudSpacer />
<div style="justify-self: end;" @onclick:stopPropagation="true">
    <MudIconButton Icon="@Icons.Material.Filled.AccountTree" aria-label="AccountTree" title="Настройки свойств группы" Size="Size.Small" />
</div>