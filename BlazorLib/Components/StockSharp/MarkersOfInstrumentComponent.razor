@inherits BlazorBusyComponentBaseModel

@if (IsBusyProgress)
{
    <div class="text-center">
        <div class="spinner-border" role="status">
            <span class="visually-hidden">Загрузка...</span>
        </div>
    </div>
}
else
{
    <MudSelect Disabled="IsBusyProgress" T="MarkersInstrumentStockSharpEnum" Label="Markers" MultiSelection="true" @bind-SelectedValues="_options">
        @foreach (MarkersInstrumentStockSharpEnum state in AllMarkers)
        {
            <MudSelectItem Disabled="IsBusyProgress" T="MarkersInstrumentStockSharpEnum" Value="state">@state</MudSelectItem>
        }
    </MudSelect>
}